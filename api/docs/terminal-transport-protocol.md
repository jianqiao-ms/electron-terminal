# Json message protocol between api and xterm.js terminal

### msg

Basic unit tranport between terminal and api via websocket,
Variable name using "msg"

```json
{
    "type": "stdin",
    "data": ""
}
```